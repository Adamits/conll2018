import os

DATA='/adam/conll2018/task1/all/'

if __name__=='__main__':
    settings_dict = {"low": {}, "medium": [], "high": []}
    for fn in os.listdirectory(DATA):
        setting = fn.split('-')[-1]
        name = fn.split('-')[:-2]
        settings_dict[setting].append(name)

    for s, langs in settings_dict.iter():
        print("====%s====" % s.upper())
        for l in sorted(lang):
            print (l)
